import { UserProvider.Model } from './user-provider.model';

describe('UserProvider.Model', () => {
  it('should create an instance', () => {
    expect(new UserProvider.Model()).toBeTruthy();
  });
});
